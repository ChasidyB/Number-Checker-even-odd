# Number-Checker-even-odd
A simple Python program that asks the user for a number and tells them whether it’s even or odd. Includes input validation and a loop so the user can check multiple numbers.
